import React from 'react'
import Layout from '../components/layout'

export default function about() {
  return (
    <Layout>
      <div>
        <p>Urea is essentially the waste produced when the body metabolizes protein. It is not only produced by humans, but also by many other mammals, as </p>
        <p>Urea is essentially the waste produced when the body metabolizes protein. It is not only produced by humans, but also by many other mammals, as </p>
      </div>
    </Layout>
  )
}
